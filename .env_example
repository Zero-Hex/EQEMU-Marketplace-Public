# EQEMU Marketplace - Environment Configuration
#
# IMPORTANT SETUP STEPS:
# 1. Copy this file to .env in the same directory: cp .env_example .env
# 2. Update all values below with your actual configuration
# 3. Generate a secure JWT_SECRET: openssl rand -hex 32
# 4. NEVER commit .env file to git (it contains passwords!)
#
# This file should be located at: /path/to/marketplace/.env (NOT in public/ directory)

# ============================================================================
# Database Configuration (REQUIRED)
# ============================================================================
DB_HOST=localhost
DB_NAME=peq
DB_USER=your_mysql_username
DB_PASS=your_mysql_password

# ============================================================================
# Security Configuration (REQUIRED)
# ============================================================================
# Generate a secure random string with: openssl rand -hex 32
JWT_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_STRING
JWT_EXPIRATION=86400

# Application Settings
SITE_URL=http://localhost/eqemu-marketplace
API_URL=http://localhost/eqemu-marketplace/api

# Listing Settings
LISTING_EXPIRATION_DAYS=7
MAX_LISTINGS_PER_USER=50
MIN_LISTING_PRICE=1

# Feature Flags
ENABLE_AUTO_REFRESH=true
REFRESH_INTERVAL_SECONDS=30
ENABLE_ITEM_ICONS=true

# Item Icons Configuration
# Leave ICON_BASE_URL empty to use default emoji icons
# Or set to your icon server URL (e.g., https://yourserver.com/icons)
ICON_BASE_URL=
DEFAULT_ICON=🎒

# Pagination
ITEMS_PER_PAGE=20

# Currency Settings
COPPER_TO_PLATINUM=1000

# Alternate Currency Configuration (Optional High-Value Currency System)
# Set to false to use platinum-only marketplace (default recommended)
# Set to true to enable custom alternate currency for high-value transactions
USE_ALT_CURRENCY=false
ALT_CURRENCY_ITEM_ID=147623
ALT_CURRENCY_VALUE_PLATINUM=1000000
ALT_CURRENCY_NAME=Alt Currency

# Development
DEBUG_MODE=true
LOG_ERRORS=true

# CORS Settings (for production, set to your domain)
ALLOWED_ORIGIN=*
